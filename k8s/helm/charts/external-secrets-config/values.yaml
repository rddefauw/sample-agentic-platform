# Environment (dev, staging, prod)
environment: dev

# AWS configuration
aws:
  region: ""  # Will be set during deployment

# Define the parameter store configuration
parameterStore:
  name: aws-parameter-store
  namespace: default
  basePath: /agentic-platform/config
